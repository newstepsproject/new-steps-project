import { SITE_CONFIG } from '@/constants/config';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: `Request Shoes | ${SITE_CONFIG.name}`,
  description: 'Browse available sports shoes and request what you need. Quality sports footwear at no cost.',
}; 